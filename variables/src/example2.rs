// Fix the variable definition of 'x'

fn main() {
    // Define a mutable variable
    let mut x = 3;
    println!("Number {}", x);
    x = 5; // don't change this line
    println!("Number {}", x);
}